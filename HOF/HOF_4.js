const multiplyBy = (num1) => {
    return function (num2) {
      return num1 * num2;
    }
  }
  
  //const multiplyByTwo = multiplyBy(2);
  //multiplyByTwo(4)
  //const multiplyByThree = multiplyBy(3);
  
  
//   for(let i= 1; i<=10; i++)
//   {
//      console.log(multiplyByThree(i));
//   }
  //const multiplyBy = (num1) => (mun2) => num1 * num2; 